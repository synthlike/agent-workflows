schema_version: 3

distribution:
  source: github.com/synthlike/agent-workflows
  version: v0.5.0

installation:
  selected:
    - author-specification
    - capture-meeting
    - capture-regression
    - clarify-intent
    - close-initiative
    - develop-rfc
    - establish-technical-baseline
    - frame-product-problem
    - investigate-failure
    - model-domain
    - plan-implementation
    - plan-initiative
    - prepare-handoff
    - prepare-questionnaire
    - prototype-design
    - record-arp
    - research-question
    - review-implementation
    - triage-issue
  skills:
    author-specification: skills/author-specification
    capture-meeting: skills/capture-meeting
    capture-regression: skills/capture-regression
    clarify-intent: skills/clarify-intent
    close-initiative: skills/close-initiative
    configure-workflows: skills/configure-workflows
    develop-rfc: skills/develop-rfc
    establish-technical-baseline: skills/establish-technical-baseline
    frame-product-problem: skills/frame-product-problem
    investigate-failure: skills/investigate-failure
    model-domain: skills/model-domain
    plan-implementation: skills/plan-implementation
    plan-initiative: skills/plan-initiative
    prepare-handoff: skills/prepare-handoff
    prepare-questionnaire: skills/prepare-questionnaire
    prototype-design: skills/prototype-design
    record-arp: skills/record-arp
    research-question: skills/research-question
    review-implementation: skills/review-implementation
    triage-issue: skills/triage-issue

backends:
  local:
    type: local-markdown

records:
  issues:
    enabled: true
    backend: local
    destination: {root: .project}
  domain:
    enabled: true
    backend: local
    destination: {path: docs/domain}
  arps:
    enabled: true
    backend: local
    destination: {path: docs/decisions, prefix: ARP}
  rfcs:
    enabled: true
    backend: local
    destination: {path: docs/rfcs, prefix: RFC}
  specs:
    enabled: true
    backend: local
    destination: {path: docs/specs}
  meetings:
    enabled: false
    backend: local
    destination: {path: docs/meetings}
  research:
    enabled: true
    backend: local
    destination: {path: docs/research}
  questionnaires:
    enabled: true
    backend: local
    destination: {path: docs/questionnaires}
  technical_baselines:
    enabled: true
    backend: local
    destination: {path: docs/engineering}
  problem_framing:
    enabled: true
    backend: local
    destination: {path: docs/product}
  prototypes:
    enabled: false
    backend: local
    destination: {path: docs/prototypes}
  handoffs:
    enabled: false
    backend: local
    destination: {path: .agents/handoffs}
